# 指導カルテ データ仕様（型）

この文書は、アプリが扱うデータの決まりを定めたものです。
将来アプリ化したり、AIに改修を頼むときは、この文書を渡せば構造が伝わります。

**最終更新：2026年9月14日 / schema 1**

---

## 1. 全体の形

データは1つのJSONオブジェクトです。トップレベルの項目は5つだけです。

```json
{
  "schema": 1,
  "students": [],
  "records": [],
  "tags": [],
  "reasons": []
}
```

| 項目 | 型 | 必須 | 意味 |
|---|---|---|---|
| `schema` | 数値 | 任意 | データ構造の版。無い場合は 1 とみなす |
| `students` | 配列 | **必須** | 生徒の一覧 |
| `records` | 配列 | **必須** | 記録した問題の一覧 |
| `tags` | 配列 | 任意 | 選択肢として使える分野タグ |
| `reasons` | 配列 | 任意 | つまずいた理由の選択肢。無い場合は初期値を補う |

読み込み時、`students` と `records` が配列でなければ不正なデータとして拒否します。

---

## 2. students（生徒）

```json
{
  "id": "s1788699819333",
  "name": "生徒Ko",
  "note": "工業高１/難関国公立志望/理系科目（物理・化学）"
}
```

| 項目 | 型 | 必須 | 決まり |
|---|---|---|---|
| `id` | 文字列 | **必須** | 一意。`s` + 作成時刻のミリ秒。**一度決めたら変えない** |
| `name` | 文字列 | **必須** | 表示名。**本名を入れない**（偽名・記号で運用する） |
| `note` | 文字列 | 任意 | 学年・志望・状況などの自由記述 |

`id` が記録との紐づけに使われるため、これを変更すると過去の記録が孤立します。

---

## 3. records（記録）

```json
{
  "id": "r1788700123456",
  "sid": "s1788699819333",
  "date": "2026-09-14",
  "src": "青チャート数I 例題52",
  "tags": ["数学:関数:二次関数", "共通:問題文の読み違え"],
  "correct": false,
  "reason": "解き方の方針が立たなかった",
  "memo": "平方完成はできるが場合分けを忘れる"
}
```

| 項目 | 型 | 必須 | 決まり |
|---|---|---|---|
| `id` | 文字列 | **必須** | 一意。`r` + 作成時刻のミリ秒。重複判定に使う |
| `sid` | 文字列 | **必須** | `students[].id` を参照 |
| `date` | 文字列 | **必須** | `YYYY-MM-DD` 形式 |
| `src` | 文字列 | 任意 | 出典。**問題文そのものは入れない**（後述） |
| `tags` | 配列 | 任意 | 分野タグ。0個でも可 |
| `correct` | 真偽値 | **必須** | `true` = 正解、`false` = 不正解 |
| `reason` | 文字列 | 任意 | `correct` が `false` のときだけ入れる。`reasons` の値のいずれか |
| `memo` | 文字列 | 任意 | 観察した様子。20〜40字程度 |

---

## 4. tags（分野タグ）

`科目:単元:項目` の3階層を基本とします。区切りは半角コロンです。

```
英語:長文:内容一致
中学理科:1年:光の性質
共通:基礎の抜け          ← 2階層も可
```

**決まり**

- 階層は**2段または3段**。1段（コロンなし）は旧形式とみなす
- 第1階層は科目名を**略さず**書く（`英` ではなく `英語`）
- 科目をまたぐつまずきは `共通:` を頭に付け、2階層にする
- 同じ文字列の重複は持たない

**階層を分ける理由**

タグが数百個になると、一覧から選ぶのが現実的でなくなります。階層にしておけば、科目 → 単元 → 項目と辿ることで、どの画面も10個以下に収まります。

**科目パック**

アプリは科目ごとのタグの束（PACKS）を内蔵しています。利用者は担当科目だけを選んで追加できます。初期状態では `tags` は空です。

現在のパック：国語 / 数学 / 英語 / 物理 / 化学 / 生物 / 地学 / 社会 / 中学国語 / 中学数学 / 中学英語 / 中学理科 / 中学社会 / 共通（合計365個）

---

## 5. reasons（つまずいた理由）

科目に依存しない8つを初期値とします。

1. 用語・知識を覚えていない
2. ルールや公式を使いこなせない
3. 問題文の条件を読み落とした
4. 解き方の方針が立たなかった
5. 途中の処理でミスした
6. 答え方が条件に合っていない
7. 時間が足りなかった
8. 手をつけられなかった

**分け方の根拠**

「何が足りなかったか」で切り分けています。それぞれ対策が異なるため、集計したときに次の手が決まります。たとえば1は暗記の反復、2は演習量、5は検算の習慣づけと、打ち手が変わります。

科目名を含まないため、国語でも物理でも同じ基準で使えます。

---

## 6. 保存と受け渡し

**保存先**

| 動作環境 | 保存先 |
|---|---|
| 通常のブラウザ | `localStorage`（キー `juku-karte-v1`） |
| Claude内 | Claudeの保存領域 |

サーバーには送信しません。端末ごとに独立します。

**受け渡し**

JSONファイルの書き出し・読み込みで行います。読み込みは**追加方式**です。

- `students` / `records` … `id` が既にあるものは無視（重複しない）
- `tags` … 既存に無いものだけ追加。「入れ替える」を選んだ場合は、記録で使用中のものを除いて置換
- `reasons` … 含まれていれば置き換え

---

## 7. 入れないもの（設計上の約束）

以下は、どの項目にも保存しません。

**問題文そのもの・その画像**

塾は営利目的の教育機関であり、著作権法35条（学校等での複製）の対象外です。入試問題や市販教材の中身は蓄積せず、出典表記と分野タグだけを残します。

写真はAIに読ませる一時的な入力として扱い、読み取り後に破棄します。

**生徒の本名・連絡先・その他の個人情報**

塾のシステムにある個人情報を、個人が作ったアプリに持ち出さないためです。記録されるのは「どの分野で、どう間違えたか」であり、個人が特定できなくても分析は成立します。

**AIが生成した提案そのもの**

対策案や練習問題はAIに都度出させます。保存するのは観察の記録だけです。再生成できるものを溜めると、データが膨らむだけで価値が増えません。

---

## 8. 版を上げるときの決まり

`schema` の数字を上げるのは、**既存データの読み方が変わるとき**だけです。

- 項目を追加しただけ（任意項目） → 版は上げない
- 項目名を変えた、型を変えた、必須項目を増やした → 版を上げ、読み込み時に変換処理を入れる

版を上げたら、この文書の冒頭の日付と版数も更新してください。

---

## 9. 今後の拡張で想定していること

構造を壊さずに足せるもの：

- `students[].subjects` … 担当科目の配列。タグ選択を担当科目に絞り込むため
- `records[].sec` … 所要時間（秒）
- `students[].archived` … 担当終了した生徒を隠すため

いずれも**任意項目として足せば、古いデータもそのまま読めます**。既存項目の意味を変えないことが、移行を楽にする唯一の方法です。
